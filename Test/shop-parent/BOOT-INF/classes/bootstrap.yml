spring:
  application:
    name: shop-gateway-service
  cloud:
    nacos:
      config:
        server-addr: localhost:8848
        file-extension: yaml
        shared-configs:
          - data-id: global-config.yaml
            refresh: true
      discovery:
        server-addr: localhost:8848
