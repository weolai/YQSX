# ============================================================
# 生产环境覆盖配置 (prod profile)
# 仅覆盖与安全相关的字段,其余继承 application.yml
# ============================================================
# 安全说明:
# - DB_PASSWORD 不提供默认值,缺失即启动失败
# - 数据库连接强制 useSSL=true,禁用 allowPublicKeyRetrieval
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/shop_payment?serverTimezone=Asia/Shanghai&useSSL=true&verifyServerCertificate=false&useUnicode=true&characterEncoding=utf-8
    username: ${DB_USER}
    password: ${DB_PASSWORD}
